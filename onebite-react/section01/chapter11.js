// 함수 선언
function greeting() {
  console.log('안녕하세요!!');
}

greeting();

function getArea(w, h) {
  let area = w * h;
  console.log(area);
  return area;
}
getArea(10, 20);
getArea(100, 200);

// 호이스팅
// -> 끌어올리다 라는 뜻
